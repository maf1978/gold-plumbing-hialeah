{
  "name": "gold-plumbing-hialeah",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=gold-plumbing-hialeah",
    "deploy:preview": "npm run build && wrangler pages deploy dist --project-name=gold-plumbing-hialeah --branch=preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.4",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
